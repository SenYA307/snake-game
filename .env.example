# =============================================
# PIXEL SNAKE - PRODUCTION ENVIRONMENT CONFIG
# =============================================
#
# FOR LOCAL DEVELOPMENT:
#   cp .env.example .env.local
#   Fill in the values below
#   Run: npm run dev
#
# FOR VERCEL DEPLOYMENT:
#   Add these as Environment Variables in your Vercel project settings
#   Dashboard → Settings → Environment Variables
#
# =============================================

# ─────────────────────────────────────────────
# REQUIRED VARIABLES (app won't work without these)
# ─────────────────────────────────────────────

# TREASURY_ADDRESS (REQUIRED)
# Your wallet address to receive payments
# Must be checksummed (mixed case) - get it from: https://ethsum.netlify.app/
TREASURY_ADDRESS=0xYourChecksummedAddressHere

# PAYMENT_TOKEN_SECRET (REQUIRED in production)
# Secret for signing payment tokens - KEEP THIS PRIVATE!
# Generate with: openssl rand -hex 32
# ⚠️ NEVER commit real secrets to git
# ⚠️ Use different secrets for each environment
PAYMENT_TOKEN_SECRET=generate-a-random-secret-with-openssl-rand-hex-32

# NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID (REQUIRED)
# Get free at: https://cloud.walletconnect.com/
# 1. Create account → New Project → Copy Project ID
# Without this, WalletConnect and mobile wallets won't work
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=your_walletconnect_project_id

# ─────────────────────────────────────────────
# OPTIONAL VARIABLES (have sensible defaults)
# ─────────────────────────────────────────────

# BASE_RPC_URL (optional, has default)
# RPC endpoint for Base mainnet verification
# Default: https://mainnet.base.org
#
# For production, consider using a paid RPC for reliability:
#   Alchemy: https://base-mainnet.g.alchemy.com/v2/YOUR_API_KEY
#   QuickNode: https://YOUR_ENDPOINT.base-mainnet.quiknode.pro/
BASE_RPC_URL=https://mainnet.base.org

# ─────────────────────────────────────────────
# VERCEL DEPLOYMENT CHECKLIST
# ─────────────────────────────────────────────
#
# 1. Push code to GitHub
# 2. Import project in Vercel (vercel.com/new)
# 3. Add Environment Variables:
#    - TREASURY_ADDRESS (your wallet)
#    - PAYMENT_TOKEN_SECRET (generate new one!)
#    - NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID
#    - BASE_RPC_URL (optional, for reliability)
# 4. Deploy!
# 5. Share the URL with friends
#
# =============================================
